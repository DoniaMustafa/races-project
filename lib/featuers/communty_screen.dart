import 'package:flutter/material.dart';

class CommuntyScreen extends StatelessWidget {
  const CommuntyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return   Container(
      alignment: Alignment.center,
      child: Text('communty Screen'),
    );
  }
}
